#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first integer to multiplied to b
 * @b: second int to multiplied to a
 * Return: returns product of int a and b
 */
int mul(int a, int b)
{
	int c;

	c = (a * b);

	return (c);
}
