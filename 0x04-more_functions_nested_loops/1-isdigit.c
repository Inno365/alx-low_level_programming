#include "main.h"

/**
 * _isdigit - Method that checks for a digits (from 0 to 9)
 * @c: To checked
 * Return: will return 1 if c is a digit else return 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
